const openBtn=document.querySelector('#open-modal');
// const closebtn=docemunt.querySelector('#close-modal');

openBtn.addEventListener('click',()=>dialog.showModal());
closebtn.addEventListener('click', ()=> dialog.close());

